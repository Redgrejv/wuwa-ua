# wuwa-ua Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Демон, що читає англійські діалогові субтитри Wuthering Waves з екрана й показує український переклад у реальному часі, не торкаючись файлів і процесу гри.

**Architecture:** Конвеєр `захоплення екрана → детектор змін → OCR → кеш/переклад → вивід`. Захоплення через ScreenCast-портал і PipeWire, OCR через tesseract за замінним інтерфейсом, переклад локальною моделлю в ollama, вивід у вікно на сусідньому моніторі або layer-shell оверлеєм. Чисті модулі (детектор, нормалізація, кеш, глосарій) покриваються юніт-тестами; захоплення й вивід перевіряються вручну.

**Tech Stack:** Python 3.12+, NumPy, OpenCV, pytesseract, PyGObject (GTK4 + GStreamer + Gio), ollama HTTP API, SQLite, pytest.

## Global Constraints

- Специфікація: `docs/superpowers/specs/2026-09-05-wuwa-ua-ocr-translation-design.md`. Вона головна; при розбіжності з планом — питати, а не вигадувати.
- **Гру не чіпати.** Жодного читання/запису у `~/Games/steamapps/common/Wuthering Waves`, жодного доступу до її процесу. Тільки фреймбуфер через портал.
- **Коміти не робити.** Кожна задача завершується `git add`; коміт виконує користувач на власний запит.
- Повні анотації типів у всіх функціях і методах, включно з поверненням.
- Коментарів у коді не писати.
- Пороги за замовчуванням із специфікації: семплювання 4 кадри/с, поріг зміни 3.0, гейт стабілізації 2 кадри, мінімальна упевненість OCR 60.0, таймаут перекладу 4.0 с, контекст 3 репліки.
- Шляхи: конфіг `~/.config/wuwa-ua/config.toml`, глосарій `~/.config/wuwa-ua/glossary.tsv`, кеш `~/.local/share/wuwa-ua/cache.sqlite`, історія `~/.local/share/wuwa-ua/history.jsonl`, сокет `$XDG_RUNTIME_DIR/wuwa-ua.sock`.
- Тести запускати з кореня репозиторію: `python -m pytest`.

---

## Структура файлів

```
pyproject.toml
src/wuwa_ua/
  types.py              дата-класи, спільні для всіх модулів
  config.py             читання й валідація TOML
  detect.py             детектор змін + гейт стабілізації
  normalize.py          чистка OCR-виводу
  ocr/base.py           протокол OcrBackend
  ocr/tesseract.py      препроцесинг + tesseract
  translate/errors.py   TranslationUnavailable
  translate/cache.py    SQLite-кеш
  translate/glossary.py глосарій + збірка промпту
  translate/ollama.py   клієнт локальної моделі
  capture/portal.py     ScreenCast-портал + PipeWire
  region.py             зони екрана, калібрування
  display/base.py       протокол Display
  display/window.py     вікно на сусідньому моніторі
  display/overlay.py    layer-shell оверлей
  control.py            unix-сокет керування
  pipeline.py           склеювання конвеєра
  cli.py                команди
tests/
  test_config.py test_detect.py test_normalize.py
  test_cache.py test_glossary.py test_ollama.py test_ocr_preprocess.py
  ocr_fixtures/         скріншоти субтитрів + еталонний текст
  test_ocr_accuracy.py  харнес точності OCR
```

---

## Фаза 1 — чисте ядро (тестується повністю)

### Task 1: Каркас проєкту й конфіг

**Files:**
- Create: `pyproject.toml`, `src/wuwa_ua/__init__.py`, `src/wuwa_ua/types.py`, `src/wuwa_ua/config.py`
- Test: `tests/test_config.py`

**Interfaces:**
- Consumes: нічого
- Produces: `Region`, `Frame`, `OcrResult`, `SubtitleLine` із `types.py`; `Config`, `load_config(path: Path) -> Config`, `ConfigError`

- [ ] **Step 1: Доставити системні залежності**

```bash
sudo pacman -S --needed tesseract-data-eng python-opencv python-gobject gtk4-layer-shell gst-plugin-pipewire ollama
tesseract --list-langs
```

Очікувано: у списку зʼявився `eng`.

`opencv` і `PyGObject` беруться **тільки** з системних пакетів — у `pyproject.toml` їх немає. Тому venv створюється з доступом до системних пакетів:

```bash
cd ~/wuwa-ua
python -m venv --system-site-packages .venv
.venv/bin/pip install -e '.[dev]'
.venv/bin/python -c "import cv2, gi, numpy, pytesseract, requests; print('ok')"
```

Далі в усіх задачах `python` означає `~/wuwa-ua/.venv/bin/python`.

- [ ] **Step 2: Створити `pyproject.toml`**

```toml
[project]
name = "wuwa-ua"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["numpy", "pytesseract", "requests"]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
wuwa-ua = "wuwa_ua.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
```

- [ ] **Step 3: Написати падаючий тест конфігу**

`tests/test_config.py`:

```python
from pathlib import Path

import pytest

from wuwa_ua.config import Config, ConfigError, load_config


def write(tmp_path: Path, body: str) -> Path:
    path = tmp_path / "config.toml"
    path.write_text(body, encoding="utf-8")
    return path


def test_loads_region_and_applies_defaults(tmp_path: Path) -> None:
    path = write(
        tmp_path,
        """
        [region]
        monitor = "DP-4"
        x = 0.25
        y = 0.80
        width = 0.50
        height = 0.12
        """,
    )

    config = load_config(path)

    assert isinstance(config, Config)
    assert config.region.monitor == "DP-4"
    assert config.region.x == 0.25
    assert config.sample_fps == 4.0
    assert config.change_threshold == 3.0
    assert config.stable_frames == 2
    assert config.min_confidence == 60.0
    assert config.translate_timeout == 4.0
    assert config.context_lines == 3
    assert config.display_mode == "window"


def test_overrides_defaults(tmp_path: Path) -> None:
    path = write(
        tmp_path,
        """
        [region]
        monitor = "DP-4"
        x = 0.0
        y = 0.0
        width = 1.0
        height = 1.0

        [detect]
        change_threshold = 5.5
        stable_frames = 3

        [display]
        mode = "overlay"
        """,
    )

    config = load_config(path)

    assert config.change_threshold == 5.5
    assert config.stable_frames == 3
    assert config.display_mode == "overlay"


def test_missing_region_is_an_error(tmp_path: Path) -> None:
    path = write(tmp_path, "[detect]\nstable_frames = 2\n")

    with pytest.raises(ConfigError, match="region"):
        load_config(path)


def test_region_outside_unit_square_is_an_error(tmp_path: Path) -> None:
    path = write(
        tmp_path,
        """
        [region]
        monitor = "DP-4"
        x = 0.9
        y = 0.0
        width = 0.5
        height = 0.1
        """,
    )

    with pytest.raises(ConfigError, match="межі"):
        load_config(path)


def test_unknown_display_mode_is_an_error(tmp_path: Path) -> None:
    path = write(
        tmp_path,
        """
        [region]
        monitor = "DP-4"
        x = 0.0
        y = 0.0
        width = 1.0
        height = 1.0

        [display]
        mode = "hologram"
        """,
    )

    with pytest.raises(ConfigError, match="hologram"):
        load_config(path)


def test_missing_file_is_an_error(tmp_path: Path) -> None:
    with pytest.raises(ConfigError, match="не знайдено"):
        load_config(tmp_path / "nope.toml")
```

- [ ] **Step 4: Переконатись, що тест падає**

Run: `python -m pytest tests/test_config.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.config'`

- [ ] **Step 5: Написати `types.py`**

```python
from __future__ import annotations

from dataclasses import dataclass

import numpy as np


@dataclass(frozen=True)
class Region:
    monitor: str
    x: float
    y: float
    width: float
    height: float

    def pixels(self, frame_width: int, frame_height: int) -> tuple[int, int, int, int]:
        left = int(self.x * frame_width)
        top = int(self.y * frame_height)
        right = int((self.x + self.width) * frame_width)
        bottom = int((self.y + self.height) * frame_height)
        return left, top, right, bottom


@dataclass(frozen=True)
class Frame:
    image: np.ndarray
    timestamp: float


@dataclass(frozen=True)
class OcrResult:
    text: str
    confidence: float


@dataclass(frozen=True)
class SubtitleLine:
    source: str
    translation: str
    translated: bool
    timestamp: float
```

- [ ] **Step 6: Написати `config.py`**

```python
from __future__ import annotations

import tomllib
from dataclasses import dataclass
from pathlib import Path
from typing import Any

from wuwa_ua.types import Region

DISPLAY_MODES = ("window", "overlay")


class ConfigError(Exception):
    pass


@dataclass(frozen=True)
class Config:
    region: Region
    sample_fps: float
    change_threshold: float
    stable_frames: int
    min_confidence: float
    translate_timeout: float
    context_lines: int
    display_mode: str
    display_monitor: str
    model: str
    ollama_host: str
    restore_token: str


def _section(data: dict[str, Any], name: str) -> dict[str, Any]:
    value = data.get(name, {})
    if not isinstance(value, dict):
        raise ConfigError(f"секція [{name}] має бути таблицею")
    return value


def _region(data: dict[str, Any]) -> Region:
    raw = data.get("region")
    if not isinstance(raw, dict):
        raise ConfigError("у конфізі відсутня секція [region]")
    try:
        region = Region(
            monitor=str(raw["monitor"]),
            x=float(raw["x"]),
            y=float(raw["y"]),
            width=float(raw["width"]),
            height=float(raw["height"]),
        )
    except KeyError as exc:
        raise ConfigError(f"у секції [region] відсутнє поле {exc.args[0]}") from exc
    if region.width <= 0 or region.height <= 0:
        raise ConfigError("ширина й висота [region] мають бути додатними")
    if not (0.0 <= region.x and 0.0 <= region.y):
        raise ConfigError("зона виходить за межі екрана")
    if region.x + region.width > 1.0 or region.y + region.height > 1.0:
        raise ConfigError("зона виходить за межі екрана")
    return region


def load_config(path: Path) -> Config:
    if not path.is_file():
        raise ConfigError(f"конфіг не знайдено: {path}")
    with path.open("rb") as handle:
        data = tomllib.load(handle)

    region = _region(data)
    detect = _section(data, "detect")
    ocr = _section(data, "ocr")
    translate = _section(data, "translate")
    display = _section(data, "display")
    capture = _section(data, "capture")

    mode = str(display.get("mode", "window"))
    if mode not in DISPLAY_MODES:
        raise ConfigError(f"невідомий режим виводу: {mode}")

    return Config(
        region=region,
        sample_fps=float(capture.get("sample_fps", 4.0)),
        change_threshold=float(detect.get("change_threshold", 3.0)),
        stable_frames=int(detect.get("stable_frames", 2)),
        min_confidence=float(ocr.get("min_confidence", 60.0)),
        translate_timeout=float(translate.get("timeout", 4.0)),
        context_lines=int(translate.get("context_lines", 3)),
        display_mode=mode,
        display_monitor=str(display.get("monitor", "")),
        model=str(translate.get("model", "")),
        ollama_host=str(translate.get("host", "http://127.0.0.1:11434")),
        restore_token=str(capture.get("restore_token", "")),
    )
```

- [ ] **Step 7: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_config.py -v`
Expected: PASS, 6 тестів

- [ ] **Step 8: Стейдж**

```bash
git add pyproject.toml src/wuwa_ua/__init__.py src/wuwa_ua/types.py src/wuwa_ua/config.py tests/test_config.py
```

---

### Task 2: Детектор змін і гейт стабілізації

Центральний елемент якості: без гейта стабілізації OCR ловитиме субтитри посеред посимвольної анімації набору й перекладатиме обірвані півфрази.

**Files:**
- Create: `src/wuwa_ua/detect.py`
- Test: `tests/test_detect.py`

**Interfaces:**
- Consumes: нічого
- Produces: `ChangeDetector(change_threshold: float = 3.0, stable_frames: int = 2, identical_threshold: float = 1.0)` з методом `push(crop: np.ndarray) -> np.ndarray | None` і `reset() -> None`

**Два різні пороги, а не один.** Рецензія знайшла критичну ваду початкової схеми: одне число 3.0
відповідало і на питання «чи картинка перестала змінюватись», і на питання «чи це вже інший текст».
При стисканні до 64px поріг 3.0 долає зміна ~1.2% площі, тож дві репліки, що різняться одним словом
чи пунктуацією, зливались в одну — друга не показувалась ніколи.

Тому `change_threshold` лишається для стабілізації, а порівняння з уже виданим кадром іде за окремим
`identical_threshold` (1.0) — він відсікає лише шум кодека на статичному екрані. Рішення «чи це нова
репліка» ухвалює конвеєр за **розпізнаним текстом**, а не за пікселями (див. Task 13).

- [ ] **Step 1: Написати падаючі тести**

`tests/test_detect.py`:

```python
import numpy as np

from wuwa_ua.detect import ChangeDetector


def solid(value: int) -> np.ndarray:
    return np.full((80, 400, 3), value, dtype=np.uint8)


def noisy(seed: int) -> np.ndarray:
    rng = np.random.default_rng(seed)
    return rng.integers(0, 255, size=(80, 400, 3), dtype=np.uint8)


def test_first_stable_frame_is_emitted_after_the_gate() -> None:
    detector = ChangeDetector(change_threshold=3.0, stable_frames=2)

    assert detector.push(solid(10)) is None
    assert detector.push(solid(10)) is not None


def test_unstable_frames_are_never_emitted() -> None:
    detector = ChangeDetector(change_threshold=3.0, stable_frames=2)

    for seed in range(6):
        assert detector.push(noisy(seed)) is None


def test_same_content_is_not_emitted_twice() -> None:
    detector = ChangeDetector(change_threshold=3.0, stable_frames=2)
    detector.push(solid(10))
    detector.push(solid(10))

    for _ in range(5):
        assert detector.push(solid(10)) is None


def test_new_content_is_emitted_after_stabilising() -> None:
    detector = ChangeDetector(change_threshold=3.0, stable_frames=2)
    detector.push(solid(10))
    detector.push(solid(10))

    assert detector.push(solid(200)) is None
    emitted = detector.push(solid(200))

    assert emitted is not None
    assert int(emitted.mean()) == 200


def test_noise_below_threshold_does_not_retrigger() -> None:
    detector = ChangeDetector(change_threshold=3.0, stable_frames=2)
    detector.push(solid(100))
    detector.push(solid(100))

    assert detector.push(solid(101)) is None
    assert detector.push(solid(101)) is None


def test_longer_gate_requires_more_stable_frames() -> None:
    detector = ChangeDetector(change_threshold=3.0, stable_frames=4)

    assert detector.push(solid(10)) is None
    assert detector.push(solid(10)) is None
    assert detector.push(solid(10)) is None
    assert detector.push(solid(10)) is not None


def test_reset_clears_emitted_state() -> None:
    detector = ChangeDetector(change_threshold=3.0, stable_frames=2)
    detector.push(solid(10))
    detector.push(solid(10))
    detector.reset()

    assert detector.push(solid(10)) is None
    assert detector.push(solid(10)) is not None
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_detect.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.detect'`

- [ ] **Step 3: Реалізувати `detect.py`**

```python
from __future__ import annotations

import cv2
import numpy as np

SIGNATURE_WIDTH = 64


class ChangeDetector:
    def __init__(
        self,
        change_threshold: float = 3.0,
        stable_frames: int = 2,
        identical_threshold: float = 1.0,
    ) -> None:
        self._change_threshold = change_threshold
        self._identical_threshold = identical_threshold
        self._stable_frames = max(1, stable_frames)
        self._emitted: np.ndarray | None = None
        self._pending: np.ndarray | None = None
        self._stable_count = 0

    def reset(self) -> None:
        self._emitted = None
        self._pending = None
        self._stable_count = 0

    def push(self, crop: np.ndarray) -> np.ndarray | None:
        signature = self._signature(crop)

        if self._emitted is not None and self._distance(signature, self._emitted) <= self._identical_threshold:
            self._pending = None
            self._stable_count = 0
            return None

        if self._pending is None or self._distance(signature, self._pending) > self._change_threshold:
            self._pending = signature
            self._stable_count = 1
            return None

        self._stable_count += 1
        if self._stable_count < self._stable_frames:
            return None

        self._emitted = signature
        self._pending = None
        self._stable_count = 0
        return crop

    def _signature(self, crop: np.ndarray) -> np.ndarray:
        gray = cv2.cvtColor(crop, cv2.COLOR_BGR2GRAY)
        height = max(1, int(gray.shape[0] * SIGNATURE_WIDTH / gray.shape[1]))
        resized = cv2.resize(gray, (SIGNATURE_WIDTH, height), interpolation=cv2.INTER_AREA)
        return resized.astype(np.float32)

    def _distance(self, left: np.ndarray, right: np.ndarray) -> float:
        if left.shape != right.shape:
            return float("inf")
        return float(np.abs(left - right).mean())
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_detect.py -v`
Expected: PASS, 7 тестів

- [ ] **Step 5: Стейдж**

```bash
git add src/wuwa_ua/detect.py tests/test_detect.py
```

---

### Task 3: Нормалізація OCR-виводу

**Files:**
- Create: `src/wuwa_ua/normalize.py`
- Test: `tests/test_normalize.py`

**Interfaces:**
- Consumes: нічого
- Produces: `normalize(text: str) -> str`, `is_meaningful(text: str) -> bool`, `cache_key(text: str) -> str`

- [ ] **Step 1: Написати падаючі тести**

`tests/test_normalize.py`:

```python
from wuwa_ua.normalize import cache_key, is_meaningful, normalize


def test_collapses_line_breaks_into_spaces() -> None:
    assert normalize("Rover, wait\nfor me here.") == "Rover, wait for me here."


def test_collapses_repeated_whitespace() -> None:
    assert normalize("What   do  you\t\tmean?") == "What do you mean?"


def test_strips_surrounding_whitespace() -> None:
    assert normalize("  Hello.  ") == "Hello."


def test_straightens_curly_quotes_and_dashes() -> None:
    assert normalize("“It’s fine” — he said") == '"It\'s fine" - he said'


def test_joins_hyphen_broken_words() -> None:
    assert normalize("Reso-\nnator") == "Resonator"


def test_drops_control_characters() -> None:
    assert normalize("Clear\x00 sky\x07") == "Clear sky"


def test_bare_carriage_return_becomes_a_space() -> None:
    assert normalize("Clear\rsky") == "Clear sky"


def test_dash_at_a_line_break_is_not_treated_as_a_hyphenated_word() -> None:
    assert normalize("It was late — \nand cold") == "It was late - and cold"


def test_empty_text_is_not_meaningful() -> None:
    assert is_meaningful("") is False
    assert is_meaningful("   ") is False


def test_punctuation_noise_is_not_meaningful() -> None:
    assert is_meaningful("|| .. ~") is False


def test_single_character_is_not_meaningful() -> None:
    assert is_meaningful("a") is False


def test_real_sentence_is_meaningful() -> None:
    assert is_meaningful("We should go.") is True


def test_cache_key_ignores_case_and_spacing() -> None:
    assert cache_key("  We SHOULD  go. ") == cache_key("we should go.")
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_normalize.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.normalize'`

- [ ] **Step 3: Реалізувати `normalize.py`**

```python
from __future__ import annotations

import re
import unicodedata

REPLACEMENTS = {
    "‘": "'",
    "’": "'",
    "‚": "'",
    "“": '"',
    "”": '"',
    "„": '"',
    "…": "...",
    "\xa0": " ",
}

DASHES = {
    "–": "-",
    "—": "-",
}

HYPHEN_BREAK = re.compile(r"-\s*\n\s*")
WHITESPACE = re.compile(r"\s+")
KEPT_CONTROLS = "\n\t\r"
MIN_LETTERS = 2
MIN_LETTER_RATIO = 0.5


def normalize(text: str) -> str:
    for source, target in REPLACEMENTS.items():
        text = text.replace(source, target)
    text = HYPHEN_BREAK.sub("", text)
    for source, target in DASHES.items():
        text = text.replace(source, target)
    text = "".join(
        char for char in text if unicodedata.category(char)[0] != "C" or char in KEPT_CONTROLS
    )
    text = WHITESPACE.sub(" ", text)
    return text.strip()


def is_meaningful(text: str) -> bool:
    stripped = normalize(text)
    letters = [char for char in stripped if char.isalpha()]
    if len(letters) < MIN_LETTERS:
        return False
    visible = [char for char in stripped if not char.isspace()]
    if not visible:
        return False
    return len(letters) / len(visible) >= MIN_LETTER_RATIO


def cache_key(text: str) -> str:
    return normalize(text).casefold()
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_normalize.py -v`
Expected: PASS, 11 тестів

- [ ] **Step 5: Стейдж**

```bash
git add src/wuwa_ua/normalize.py tests/test_normalize.py
```

---

### Task 4: Кеш перекладів

**Files:**
- Create: `src/wuwa_ua/translate/__init__.py`, `src/wuwa_ua/translate/cache.py`
- Test: `tests/test_cache.py`

**Interfaces:**
- Consumes: `cache_key` із `normalize.py`
- Produces: `TranslationCache(path: Path)` з методами `get(source: str) -> str | None`, `put(source: str, translation: str) -> None`, `close() -> None`

- [ ] **Step 1: Написати падаючі тести**

`tests/test_cache.py`:

```python
from pathlib import Path

from wuwa_ua.translate.cache import TranslationCache


def test_miss_returns_none(tmp_path: Path) -> None:
    cache = TranslationCache(tmp_path / "cache.sqlite")

    assert cache.get("We should go.") is None

    cache.close()


def test_stores_and_returns_translation(tmp_path: Path) -> None:
    cache = TranslationCache(tmp_path / "cache.sqlite")
    cache.put("We should go.", "Нам треба йти.")

    assert cache.get("We should go.") == "Нам треба йти."

    cache.close()


def test_lookup_ignores_case_and_spacing(tmp_path: Path) -> None:
    cache = TranslationCache(tmp_path / "cache.sqlite")
    cache.put("We should go.", "Нам треба йти.")

    assert cache.get("  we  SHOULD go. ") == "Нам треба йти."

    cache.close()


def test_second_put_overwrites(tmp_path: Path) -> None:
    cache = TranslationCache(tmp_path / "cache.sqlite")
    cache.put("We should go.", "Нам треба йти.")
    cache.put("We should go.", "Час рушати.")

    assert cache.get("We should go.") == "Час рушати."

    cache.close()


def test_survives_reopen(tmp_path: Path) -> None:
    path = tmp_path / "cache.sqlite"
    first = TranslationCache(path)
    first.put("We should go.", "Нам треба йти.")
    first.close()

    second = TranslationCache(path)

    assert second.get("We should go.") == "Нам треба йти."

    second.close()


def test_creates_parent_directory(tmp_path: Path) -> None:
    path = tmp_path / "nested" / "deeper" / "cache.sqlite"

    cache = TranslationCache(path)
    cache.put("Hi.", "Привіт.")

    assert path.is_file()

    cache.close()
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_cache.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.translate'`

- [ ] **Step 3: Реалізувати `cache.py`**

`src/wuwa_ua/translate/__init__.py` лишається порожнім.

```python
from __future__ import annotations

import sqlite3
from pathlib import Path

from wuwa_ua.normalize import cache_key

SCHEMA = """
CREATE TABLE IF NOT EXISTS translations (
    key TEXT PRIMARY KEY,
    source TEXT NOT NULL,
    translation TEXT NOT NULL
)
"""


class TranslationCache:
    def __init__(self, path: Path) -> None:
        path.parent.mkdir(parents=True, exist_ok=True)
        self._connection = sqlite3.connect(path, check_same_thread=False)
        self._connection.execute(SCHEMA)
        self._connection.commit()

    def get(self, source: str) -> str | None:
        row = self._connection.execute(
            "SELECT translation FROM translations WHERE key = ?",
            (cache_key(source),),
        ).fetchone()
        return None if row is None else str(row[0])

    def put(self, source: str, translation: str) -> None:
        self._connection.execute(
            "INSERT INTO translations (key, source, translation) VALUES (?, ?, ?) "
            "ON CONFLICT(key) DO UPDATE SET translation = excluded.translation, source = excluded.source",
            (cache_key(source), source, translation),
        )
        self._connection.commit()

    def close(self) -> None:
        self._connection.close()
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_cache.py -v`
Expected: PASS, 6 тестів

- [ ] **Step 5: Стейдж**

```bash
git add src/wuwa_ua/translate/__init__.py src/wuwa_ua/translate/cache.py tests/test_cache.py
```

---

### Task 5: Глосарій і збірка промпту

Глосарій потрібен, щоб власні назви не плавали від репліки до репліки. У промпт іде не весь файл, а тільки записи, що зустрілися в поточному рядку.

**Files:**
- Create: `src/wuwa_ua/translate/glossary.py`
- Test: `tests/test_glossary.py`

**Interfaces:**
- Consumes: нічого
- Produces: `Glossary.load(path: Path) -> Glossary`, `Glossary.entries_for(text: str) -> list[tuple[str, str]]`, `build_prompt(text: str, context: Sequence[str], entries: Sequence[tuple[str, str]]) -> str`, `SYSTEM_PROMPT: str`

- [ ] **Step 1: Написати падаючі тести**

`tests/test_glossary.py`:

```python
from pathlib import Path

from wuwa_ua.translate.glossary import Glossary, build_prompt


def write(tmp_path: Path, body: str) -> Path:
    path = tmp_path / "glossary.tsv"
    path.write_text(body, encoding="utf-8")
    return path


def test_missing_file_yields_empty_glossary(tmp_path: Path) -> None:
    glossary = Glossary.load(tmp_path / "nope.tsv")

    assert glossary.entries_for("Rover is here.") == []


def test_matches_entry_present_in_text(tmp_path: Path) -> None:
    glossary = Glossary.load(write(tmp_path, "Rover\tМандрівник\nScar\tШрам\n"))

    assert glossary.entries_for("Rover, wait.") == [("Rover", "Мандрівник")]


def test_ignores_entries_absent_from_text(tmp_path: Path) -> None:
    glossary = Glossary.load(write(tmp_path, "Rover\tМандрівник\nScar\tШрам\n"))

    assert glossary.entries_for("Nothing relevant here.") == []


def test_matching_is_case_insensitive(tmp_path: Path) -> None:
    glossary = Glossary.load(write(tmp_path, "Rover\tМандрівник\n"))

    assert glossary.entries_for("ROVER, wait.") == [("Rover", "Мандрівник")]


def test_matches_whole_words_only(tmp_path: Path) -> None:
    glossary = Glossary.load(write(tmp_path, "Scar\tШрам\n"))

    assert glossary.entries_for("She has a scarf.") == []


def test_skips_comments_and_blank_lines(tmp_path: Path) -> None:
    glossary = Glossary.load(write(tmp_path, "# коментар\n\nRover\tМандрівник\n"))

    assert glossary.entries_for("Rover.") == [("Rover", "Мандрівник")]


def test_skips_malformed_lines(tmp_path: Path) -> None:
    glossary = Glossary.load(write(tmp_path, "broken-line-without-tab\nRover\tМандрівник\n"))

    assert glossary.entries_for("Rover.") == [("Rover", "Мандрівник")]


def test_prompt_contains_the_source_text() -> None:
    prompt = build_prompt("We should go.", context=[], entries=[])

    assert "We should go." in prompt


def test_prompt_lists_glossary_entries() -> None:
    prompt = build_prompt("Rover, wait.", context=[], entries=[("Rover", "Мандрівник")])

    assert "Rover" in prompt
    assert "Мандрівник" in prompt


def test_prompt_includes_context_lines() -> None:
    prompt = build_prompt("And then?", context=["We should go.", "Not yet."], entries=[])

    assert "We should go." in prompt
    assert "Not yet." in prompt


def test_prompt_without_context_omits_the_context_block() -> None:
    prompt = build_prompt("We should go.", context=[], entries=[])

    assert "ПОПЕРЕДНІ РЕПЛІКИ" not in prompt
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_glossary.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.translate.glossary'`

- [ ] **Step 3: Реалізувати `glossary.py`**

```python
from __future__ import annotations

import re
from collections.abc import Sequence
from pathlib import Path

SYSTEM_PROMPT = (
    "Ти перекладаєш діалоги відеогри з англійської українською. "
    "Зберігай тон, регістр і довжину репліки. Не пояснюй, не коментуй, "
    "не додавай лапок від себе. Виводь ЛИШЕ переклад одним рядком."
)


class Glossary:
    def __init__(self, entries: list[tuple[str, str]]) -> None:
        self._entries = entries

    @classmethod
    def load(cls, path: Path) -> "Glossary":
        if not path.is_file():
            return cls([])
        entries: list[tuple[str, str]] = []
        for line in path.read_text(encoding="utf-8").splitlines():
            stripped = line.strip()
            if not stripped or stripped.startswith("#"):
                continue
            parts = stripped.split("\t")
            if len(parts) < 2:
                continue
            source = parts[0].strip()
            target = parts[1].strip()
            if source and target:
                entries.append((source, target))
        return cls(entries)

    def entries_for(self, text: str) -> list[tuple[str, str]]:
        matched: list[tuple[str, str]] = []
        for source, target in self._entries:
            pattern = re.compile(rf"\b{re.escape(source)}\b", re.IGNORECASE)
            if pattern.search(text):
                matched.append((source, target))
        return matched


def build_prompt(text: str, context: Sequence[str], entries: Sequence[tuple[str, str]]) -> str:
    blocks: list[str] = []
    if entries:
        lines = "\n".join(f"{source} = {target}" for source, target in entries)
        blocks.append(f"ВЛАСНІ НАЗВИ (вживай саме так):\n{lines}")
    if context:
        lines = "\n".join(context)
        blocks.append(f"ПОПЕРЕДНІ РЕПЛІКИ (тільки для контексту, не перекладай їх):\n{lines}")
    blocks.append(f"ПЕРЕКЛАДИ ЦЕ:\n{text}")
    return "\n\n".join(blocks)
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_glossary.py -v`
Expected: PASS, 11 тестів

- [ ] **Step 5: Створити стартовий глосарій**

```bash
mkdir -p ~/.config/wuwa-ua
cat > ~/.config/wuwa-ua/glossary.tsv <<'EOF'
# англійська<TAB>українська
Rover	Мандрівник
Resonator	Резонатор
Tacet Discord	Тацетний Дискорд
Sonoro Sphere	Сфера Соноро
Huanglong	Хуанлун
Jinzhou	Цзіньчжоу
Black Shores	Чорні Береги
EOF
```

- [ ] **Step 6: Стейдж**

```bash
git add src/wuwa_ua/translate/glossary.py tests/test_glossary.py
```

---

### Task 6: Клієнт ollama з таймаутом і запасним шляхом

**Files:**
- Create: `src/wuwa_ua/translate/errors.py`, `src/wuwa_ua/translate/ollama.py`
- Test: `tests/test_ollama.py`

**Interfaces:**
- Consumes: `SYSTEM_PROMPT`, `build_prompt` із `glossary.py`
- Produces: `TranslationUnavailable`, `OllamaTranslator(model: str, host: str, timeout: float)` з методом `translate(text: str, context: Sequence[str], entries: Sequence[tuple[str, str]]) -> str`

- [ ] **Step 1: Написати падаючі тести**

`tests/test_ollama.py`:

```python
from typing import Any

import pytest
import requests

from wuwa_ua.translate.errors import TranslationUnavailable
from wuwa_ua.translate.ollama import OllamaTranslator


class FakeResponse:
    def __init__(self, payload: dict[str, Any], status: int = 200) -> None:
        self._payload = payload
        self.status_code = status

    def json(self) -> dict[str, Any]:
        return self._payload

    def raise_for_status(self) -> None:
        if self.status_code >= 400:
            raise requests.HTTPError(f"status {self.status_code}")


def test_returns_the_model_reply(monkeypatch: pytest.MonkeyPatch) -> None:
    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        return FakeResponse({"response": "Нам треба йти."})

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="test", host="http://x", timeout=1.0)

    assert translator.translate("We should go.", context=[], entries=[]) == "Нам треба йти."


def test_strips_whitespace_and_wrapping_quotes(monkeypatch: pytest.MonkeyPatch) -> None:
    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        return FakeResponse({"response": '  "Нам треба йти."  '})

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="test", host="http://x", timeout=1.0)

    assert translator.translate("We should go.", context=[], entries=[]) == "Нам треба йти."


def test_keeps_only_the_first_line(monkeypatch: pytest.MonkeyPatch) -> None:
    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        return FakeResponse({"response": "Нам треба йти.\n\nПояснення: це переклад."})

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="test", host="http://x", timeout=1.0)

    assert translator.translate("We should go.", context=[], entries=[]) == "Нам треба йти."


def test_timeout_raises_translation_unavailable(monkeypatch: pytest.MonkeyPatch) -> None:
    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        raise requests.Timeout("too slow")

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="test", host="http://x", timeout=1.0)

    with pytest.raises(TranslationUnavailable):
        translator.translate("We should go.", context=[], entries=[])


def test_connection_error_raises_translation_unavailable(monkeypatch: pytest.MonkeyPatch) -> None:
    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        raise requests.ConnectionError("ollama is down")

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="test", host="http://x", timeout=1.0)

    with pytest.raises(TranslationUnavailable):
        translator.translate("We should go.", context=[], entries=[])


def test_http_error_raises_translation_unavailable(monkeypatch: pytest.MonkeyPatch) -> None:
    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        return FakeResponse({}, status=500)

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="test", host="http://x", timeout=1.0)

    with pytest.raises(TranslationUnavailable):
        translator.translate("We should go.", context=[], entries=[])


def test_empty_reply_raises_translation_unavailable(monkeypatch: pytest.MonkeyPatch) -> None:
    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        return FakeResponse({"response": "   "})

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="test", host="http://x", timeout=1.0)

    with pytest.raises(TranslationUnavailable):
        translator.translate("We should go.", context=[], entries=[])


def test_request_carries_model_prompt_and_timeout(monkeypatch: pytest.MonkeyPatch) -> None:
    captured: dict[str, Any] = {}

    def fake_post(url: str, json: dict[str, Any], timeout: float) -> FakeResponse:
        captured["url"] = url
        captured["json"] = json
        captured["timeout"] = timeout
        return FakeResponse({"response": "Мандрівнику, зачекай."})

    monkeypatch.setattr(requests, "post", fake_post)
    translator = OllamaTranslator(model="gemma3:4b", host="http://x", timeout=2.5)
    translator.translate("Rover, wait.", context=["Not yet."], entries=[("Rover", "Мандрівник")])

    assert captured["url"] == "http://x/api/generate"
    assert captured["json"]["model"] == "gemma3:4b"
    assert captured["json"]["stream"] is False
    assert "Rover, wait." in captured["json"]["prompt"]
    assert "Мандрівник" in captured["json"]["prompt"]
    assert "Not yet." in captured["json"]["prompt"]
    assert captured["timeout"] == 2.5
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_ollama.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.translate.errors'`

- [ ] **Step 3: Реалізувати `errors.py` і `ollama.py`**

`src/wuwa_ua/translate/errors.py`:

```python
from __future__ import annotations


class TranslationUnavailable(Exception):
    pass
```

`src/wuwa_ua/translate/ollama.py`:

```python
from __future__ import annotations

from collections.abc import Sequence

import requests

from wuwa_ua.translate.errors import TranslationUnavailable
from wuwa_ua.translate.glossary import SYSTEM_PROMPT, build_prompt


class OllamaTranslator:
    def __init__(self, model: str, host: str = "http://127.0.0.1:11434", timeout: float = 4.0) -> None:
        self._model = model
        self._host = host.rstrip("/")
        self._timeout = timeout

    def translate(
        self,
        text: str,
        context: Sequence[str],
        entries: Sequence[tuple[str, str]],
    ) -> str:
        payload = {
            "model": self._model,
            "system": SYSTEM_PROMPT,
            "prompt": build_prompt(text, context, entries),
            "stream": False,
            "options": {"temperature": 0.2},
        }
        try:
            response = requests.post(f"{self._host}/api/generate", json=payload, timeout=self._timeout)
            response.raise_for_status()
            raw = str(response.json().get("response", ""))
        except (requests.RequestException, ValueError) as exc:
            raise TranslationUnavailable(str(exc)) from exc

        cleaned = self._clean(raw)
        if not cleaned:
            raise TranslationUnavailable("порожня відповідь моделі")
        return cleaned

    def _clean(self, raw: str) -> str:
        first = raw.strip().splitlines()
        if not first:
            return ""
        line = first[0].strip()
        if len(line) >= 2 and line[0] == '"' and line[-1] == '"':
            line = line[1:-1].strip()
        return line
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_ollama.py -v`
Expected: PASS, 8 тестів

- [ ] **Step 5: Стейдж**

```bash
git add src/wuwa_ua/translate/errors.py src/wuwa_ua/translate/ollama.py tests/test_ollama.py
```

---

## Фаза 2 — OCR

### Task 7: Препроцесинг, бекенд tesseract і харнес точності

Головний технічний ризик проєкту — стилізований шрифт гри. Харнес точності з цієї задачі і є тим інструментом, що дасть відповідь, чи вистачає tesseract, чи вмикати VLM.

**Польова перевірка (05.09.2026) спростувала початкове припущення.** Напівпрозорої плашки може не
бути взагалі — текст лежить прямо на сцені, — а фон буває і темним, і світлим. Тому ні глобальний
Отсу, ні поріг за яскравістю не годяться: обидва залежать від яскравості фону.

Препроцесинг тепер будується на морфологічному top-hat. Він виділяє яскраві структури, тонші за
ядро (штрихи літер), і придушує все, що змінюється повільніше за ядро (фон) — незалежно від того,
темний фон чи світлий. Після top-hat фон стає рівномірно темним, і Отсу вже працює за призначенням;
результат інвертується у темний текст на білому, як любить tesseract.

`TOPHAT_KERNEL` має бути більшим за товщину штриха, але меншим за деталі фону. Стартове значення 15
підбирається на фікстурах у кроці 6.

**Files:**
- Create: `src/wuwa_ua/ocr/__init__.py`, `src/wuwa_ua/ocr/base.py`, `src/wuwa_ua/ocr/tesseract.py`, `tests/ocr_fixtures/README.md`
- Test: `tests/test_ocr_preprocess.py`, `tests/test_ocr_accuracy.py`

**Interfaces:**
- Consumes: `OcrResult` із `types.py`, `normalize` із `normalize.py`
- Produces: `OcrBackend` (Protocol із методом `recognize(image: np.ndarray) -> OcrResult`), `preprocess(crop: np.ndarray) -> np.ndarray`, `TesseractOcr(lang: str = "eng", min_confidence: float = 60.0)`

- [ ] **Step 1: Написати падаючі тести препроцесингу**

`tests/test_ocr_preprocess.py`:

```python
import cv2
import numpy as np

from wuwa_ua.ocr.tesseract import UPSCALE, preprocess


def light_text_on_dark_bar() -> np.ndarray:
    image = np.full((60, 300, 3), 20, dtype=np.uint8)
    cv2.putText(image, "Hello", (10, 42), cv2.FONT_HERSHEY_SIMPLEX, 1.2, (235, 235, 235), 2)
    return image


def test_output_is_single_channel() -> None:
    result = preprocess(light_text_on_dark_bar())

    assert result.ndim == 2


def test_output_is_upscaled() -> None:
    source = light_text_on_dark_bar()

    result = preprocess(source)

    assert result.shape[0] == source.shape[0] * UPSCALE
    assert result.shape[1] == source.shape[1] * UPSCALE


def test_output_is_binary() -> None:
    result = preprocess(light_text_on_dark_bar())

    assert set(np.unique(result).tolist()) <= {0, 255}


def test_light_text_becomes_dark_on_light_background() -> None:
    result = preprocess(light_text_on_dark_bar())

    assert result.mean() > 127
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_ocr_preprocess.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.ocr'`

- [ ] **Step 3: Реалізувати `base.py` і `tesseract.py`**

`src/wuwa_ua/ocr/__init__.py` лишається порожнім.

`src/wuwa_ua/ocr/base.py`:

```python
from __future__ import annotations

from typing import Protocol

import numpy as np

from wuwa_ua.types import OcrResult


class OcrBackend(Protocol):
    def recognize(self, image: np.ndarray) -> OcrResult: ...
```

`src/wuwa_ua/ocr/tesseract.py`:

```python
from __future__ import annotations

import cv2
import numpy as np
import pytesseract

from wuwa_ua.normalize import normalize
from wuwa_ua.types import OcrResult

UPSCALE = 2
TOPHAT_KERNEL = 15
TESSERACT_CONFIG = "--oem 3 --psm 6"


def preprocess(crop: np.ndarray) -> np.ndarray:
    gray = cv2.cvtColor(crop, cv2.COLOR_BGR2GRAY)
    scaled = cv2.resize(gray, None, fx=UPSCALE, fy=UPSCALE, interpolation=cv2.INTER_CUBIC)
    kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (TOPHAT_KERNEL, TOPHAT_KERNEL))
    tophat = cv2.morphologyEx(scaled, cv2.MORPH_TOPHAT, kernel)
    blurred = cv2.bilateralFilter(tophat, 5, 50, 50)
    _, binary = cv2.threshold(blurred, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
    return cv2.bitwise_not(binary)


class TesseractOcr:
    def __init__(self, lang: str = "eng", min_confidence: float = 60.0) -> None:
        self._lang = lang
        self._min_confidence = min_confidence

    def recognize(self, image: np.ndarray) -> OcrResult:
        prepared = preprocess(image)
        data = pytesseract.image_to_data(
            prepared,
            lang=self._lang,
            config=TESSERACT_CONFIG,
            output_type=pytesseract.Output.DICT,
        )

        words: list[str] = []
        confidences: list[float] = []
        for word, confidence in zip(data["text"], data["conf"], strict=False):
            value = float(confidence)
            if not word.strip() or value < 0:
                continue
            words.append(word)
            confidences.append(value)

        if not words:
            return OcrResult(text="", confidence=0.0)

        return OcrResult(text=normalize(" ".join(words)), confidence=sum(confidences) / len(confidences))
```

- [ ] **Step 4: Переконатись, що тести препроцесингу проходять**

Run: `python -m pytest tests/test_ocr_preprocess.py -v`
Expected: PASS, 4 тести

- [ ] **Step 5: Написати харнес точності**

`tests/ocr_fixtures/README.md`:

```markdown
Кожна фікстура — пара файлів з однаковою назвою:

- `<name>.png` — кроп зони субтитрів, знятий із гри
- `<name>.txt` — точний англійський текст із цього кропу, один рядок

Харнес рахує посимвольну відстань Левенштейна між еталоном і виводом OCR.
Мінімум для змістовного заміру — 15 фікстур: світлі й темні сцени,
короткі й довгі репліки, репліки з власними назвами.
```

`tests/test_ocr_accuracy.py`:

```python
from pathlib import Path

import cv2
import pytest

from wuwa_ua.normalize import normalize
from wuwa_ua.ocr.tesseract import TesseractOcr

FIXTURES = Path(__file__).parent / "ocr_fixtures"
MAX_ERROR_RATE = 0.10


def levenshtein(left: str, right: str) -> int:
    previous = list(range(len(right) + 1))
    for i, left_char in enumerate(left, start=1):
        current = [i]
        for j, right_char in enumerate(right, start=1):
            current.append(
                min(
                    previous[j] + 1,
                    current[j - 1] + 1,
                    previous[j - 1] + (left_char != right_char),
                )
            )
        previous = current
    return previous[-1]


def fixture_names() -> list[str]:
    return sorted(path.stem for path in FIXTURES.glob("*.png"))


@pytest.mark.skipif(not fixture_names(), reason="немає фікстур субтитрів")
def test_ocr_error_rate_is_acceptable() -> None:
    backend = TesseractOcr()
    total_errors = 0
    total_characters = 0
    report: list[str] = []

    for name in fixture_names():
        image = cv2.imread(str(FIXTURES / f"{name}.png"))
        expected = normalize((FIXTURES / f"{name}.txt").read_text(encoding="utf-8"))
        actual = backend.recognize(image).text
        errors = levenshtein(expected, actual)
        total_errors += errors
        total_characters += len(expected)
        report.append(f"{name}: {errors}/{len(expected)}\n  очікувано: {expected}\n  отримано:  {actual}")

    error_rate = total_errors / max(1, total_characters)
    assert error_rate <= MAX_ERROR_RATE, (
        f"частка помилок OCR {error_rate:.1%} > {MAX_ERROR_RATE:.0%}\n" + "\n".join(report)
    )
```

- [ ] **Step 6: Зібрати фікстури й запустити харнес**

Зняти щонайменше 15 кропів зони субтитрів під час гри (`Spectacle`, прямокутне виділення), покласти пари `.png`/`.txt` у `tests/ocr_fixtures/`.

Run: `python -m pytest tests/test_ocr_accuracy.py -v -s`

**Це точка рішення.** Якщо частка помилок ≤ 10% — tesseract достатній, `ocr/vlm.py` не потрібен, і його не пишемо (YAGNI). Якщо > 10% — зупинитись і повідомити результат замовнику разом із роздруком найгірших фікстур; окрема задача на VLM-бекенд планується вже за фактом.

- [ ] **Step 7: Стейдж**

```bash
git add src/wuwa_ua/ocr tests/test_ocr_preprocess.py tests/test_ocr_accuracy.py tests/ocr_fixtures
```

---

## Фаза 3 — захоплення екрана

### Task 8: ScreenCast-портал і потік кадрів

Юніт-тестами не покривається: це інтеграція з порталом і PipeWire. Верифікація — сценарієм, що зберігає кадр на диск.

**Files:**
- Create: `src/wuwa_ua/capture/__init__.py`, `src/wuwa_ua/capture/portal.py`, `scripts/capture_smoke.py`

**Interfaces:**
- Consumes: `Frame` із `types.py`
- Produces: `PortalCapture(restore_token: str = "")` з методами `start() -> None`, `frames() -> Iterator[Frame]`, `stop() -> None`, властивістю `restore_token: str`; `CaptureError`

- [ ] **Step 1: Реалізувати `portal.py`**

```python
from __future__ import annotations

import threading
import time
from collections.abc import Iterator
from queue import Empty, Queue

import gi

gi.require_version("Gst", "1.0")
from gi.repository import Gio, GLib, Gst  # noqa: E402

import numpy as np  # noqa: E402

from wuwa_ua.types import Frame  # noqa: E402

PORTAL_BUS = "org.freedesktop.portal.Desktop"
PORTAL_PATH = "/org/freedesktop/portal/desktop"
SCREENCAST = "org.freedesktop.portal.ScreenCast"
MONITOR_SOURCE = 1
PERSIST_UNTIL_REVOKED = 2
QUEUE_SIZE = 2


class CaptureError(Exception):
    pass


class PortalCapture:
    def __init__(self, restore_token: str = "") -> None:
        Gst.init(None)
        self._requested_token = restore_token
        self.restore_token = restore_token
        self._queue: Queue[Frame] = Queue(maxsize=QUEUE_SIZE)
        self._pipeline: Gst.Element | None = None
        self._loop = GLib.MainLoop()
        self._loop_thread: threading.Thread | None = None
        self._node_id: int | None = None
        self._session: str | None = None
        self._proxy = Gio.DBusProxy.new_for_bus_sync(
            Gio.BusType.SESSION, Gio.DBusProxyFlags.NONE, None,
            PORTAL_BUS, PORTAL_PATH, SCREENCAST, None,
        )

    def start(self) -> None:
        self._negotiate()
        if self._node_id is None:
            raise CaptureError("портал не повернув PipeWire-вузол")
        self._build_pipeline(self._node_id)

    def frames(self) -> Iterator[Frame]:
        while True:
            try:
                yield self._queue.get(timeout=5.0)
            except Empty as exc:
                raise CaptureError("потік кадрів зупинився") from exc

    def stop(self) -> None:
        if self._pipeline is not None:
            self._pipeline.set_state(Gst.State.NULL)
            self._pipeline = None
        if self._loop.is_running():
            self._loop.quit()

    def _negotiate(self) -> None:
        results: dict[str, GLib.Variant] = {}
        pending = GLib.MainLoop()

        def on_response(
            connection: Gio.DBusConnection, sender: str, path: str,
            interface: str, signal: str, parameters: GLib.Variant,
        ) -> None:
            code, payload = parameters.unpack()
            if code != 0:
                pending.quit()
                raise CaptureError(f"портал відхилив запит (код {code})")
            results.update(payload)
            pending.quit()

        connection = self._proxy.get_connection()

        def call(method: str, args: GLib.Variant) -> None:
            handle = self._proxy.call_sync(method, args, Gio.DBusCallFlags.NONE, -1, None).unpack()[0]
            subscription = connection.signal_subscribe(
                PORTAL_BUS, "org.freedesktop.portal.Request", "Response",
                handle, None, Gio.DBusSignalFlags.NONE, on_response,
            )
            pending.run()
            connection.signal_unsubscribe(subscription)

        call("CreateSession", GLib.Variant("(a{sv})", ({
            "session_handle_token": GLib.Variant("s", "wuwaua"),
            "handle_token": GLib.Variant("s", "wuwaua0"),
        },)))
        self._session = results["session_handle"]

        options = {
            "types": GLib.Variant("u", MONITOR_SOURCE),
            "multiple": GLib.Variant("b", False),
            "persist_mode": GLib.Variant("u", PERSIST_UNTIL_REVOKED),
            "handle_token": GLib.Variant("s", "wuwaua1"),
        }
        if self._requested_token:
            options["restore_token"] = GLib.Variant("s", self._requested_token)
        call("SelectSources", GLib.Variant("(oa{sv})", (self._session, options)))

        call("Start", GLib.Variant("(osa{sv})", (
            self._session, "", {"handle_token": GLib.Variant("s", "wuwaua2")},
        )))

        self.restore_token = str(results.get("restore_token", self._requested_token))
        streams = results.get("streams", [])
        if not streams:
            raise CaptureError("портал не повернув жодного потоку")
        self._node_id = int(streams[0][0])

    def _build_pipeline(self, node_id: int) -> None:
        description = (
            f"pipewiresrc path={node_id} ! videoconvert ! "
            "video/x-raw,format=BGR ! appsink name=sink emit-signals=true max-buffers=1 drop=true"
        )
        self._pipeline = Gst.parse_launch(description)
        sink = self._pipeline.get_by_name("sink")
        sink.connect("new-sample", self._on_sample)
        self._pipeline.set_state(Gst.State.PLAYING)
        self._loop_thread = threading.Thread(target=self._loop.run, daemon=True)
        self._loop_thread.start()

    def _on_sample(self, sink: Gst.Element) -> Gst.FlowReturn:
        sample = sink.emit("pull-sample")
        if sample is None:
            return Gst.FlowReturn.ERROR
        buffer = sample.get_buffer()
        caps = sample.get_caps().get_structure(0)
        width = caps.get_value("width")
        height = caps.get_value("height")
        success, info = buffer.map(Gst.MapFlags.READ)
        if not success:
            return Gst.FlowReturn.ERROR
        try:
            image = np.frombuffer(info.data, dtype=np.uint8).reshape((height, width, 3)).copy()
        finally:
            buffer.unmap(info)
        if self._queue.full():
            try:
                self._queue.get_nowait()
            except Empty:
                pass
        self._queue.put(Frame(image=image, timestamp=time.monotonic()))
        return Gst.FlowReturn.OK
```

- [ ] **Step 2: Написати сценарій перевірки**

`scripts/capture_smoke.py`:

```python
from __future__ import annotations

import itertools
import sys

import cv2

from wuwa_ua.capture.portal import PortalCapture


def main() -> int:
    capture = PortalCapture()
    capture.start()
    print(f"restore_token: {capture.restore_token}")
    for index, frame in enumerate(itertools.islice(capture.frames(), 5)):
        path = f"/tmp/wuwa-capture-{index}.png"
        cv2.imwrite(path, frame.image)
        print(f"{path}  {frame.image.shape}")
    capture.stop()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 3: Перевірити захоплення**

Run: `python scripts/capture_smoke.py`

Очікувано: зʼявляється діалог KDE з вибором монітора; після вибору в консоль друкується `restore_token` і пʼять шляхів; у `/tmp/wuwa-capture-*.png` лежать кадри обраного екрана з роздільністю 2560x1440.

- [ ] **Step 4: Записати токен у конфіг**

```bash
mkdir -p ~/.config/wuwa-ua
```

Створити `~/.config/wuwa-ua/config.toml` із секцією `[capture]` і полем `restore_token` зі значенням, надрукованим на попередньому кроці. Повторний запуск `scripts/capture_smoke.py` після цього не має показувати діалог вибору монітора.

- [ ] **Step 5: Стейдж**

```bash
git add src/wuwa_ua/capture scripts/capture_smoke.py
```

---

### Task 9: Зони екрана й калібрування

**Files:**
- Create: `src/wuwa_ua/region.py`
- Modify: `src/wuwa_ua/cli.py` (створюється тут уперше)

**Interfaces:**
- Consumes: `Region` із `types.py`, `PortalCapture` із `capture/portal.py`
- Produces: `crop(image: np.ndarray, region: Region) -> np.ndarray`, `calibrate(capture: PortalCapture, monitor: str) -> Region`, `render_region_toml(region: Region) -> str`

**Чому `monitor` передається аргументом:** портал не повертає connector-імені екрана (`DP-4`), а `Region.monitor` потрібне Task 13 для розміщення оверлея. Тому користувач вказує його прапорцем `--monitor`, а список доступних імен дає `kscreen-doctor -o`.

- [ ] **Step 1: Написати падаючий тест кропу**

Створити `tests/test_region.py`:

```python
import numpy as np

from wuwa_ua.region import crop, render_region_toml
from wuwa_ua.types import Region


def test_crop_returns_the_requested_rectangle() -> None:
    image = np.zeros((1000, 2000, 3), dtype=np.uint8)
    image[800:920, 500:1500] = 255
    region = Region(monitor="DP-4", x=0.25, y=0.8, width=0.5, height=0.12)

    result = crop(image, region)

    assert result.shape == (120, 1000, 3)
    assert int(result.min()) == 255


def test_crop_of_full_frame_is_the_frame() -> None:
    image = np.zeros((100, 200, 3), dtype=np.uint8)
    region = Region(monitor="DP-4", x=0.0, y=0.0, width=1.0, height=1.0)

    assert crop(image, region).shape == image.shape


def test_rendered_toml_round_trips() -> None:
    region = Region(monitor="DP-4", x=0.25, y=0.8, width=0.5, height=0.12)

    rendered = render_region_toml(region)

    assert "[region]" in rendered
    assert 'monitor = "DP-4"' in rendered
    assert "x = 0.25" in rendered
    assert "height = 0.12" in rendered
```

- [ ] **Step 2: Переконатись, що тест падає**

Run: `python -m pytest tests/test_region.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.region'`

- [ ] **Step 3: Реалізувати `region.py`**

```python
from __future__ import annotations

import cv2
import numpy as np

from wuwa_ua.capture.portal import PortalCapture
from wuwa_ua.types import Region

CALIBRATION_WINDOW = "wuwa-ua: виділи зону субтитрів і натисни Enter"


def crop(image: np.ndarray, region: Region) -> np.ndarray:
    height, width = image.shape[:2]
    left, top, right, bottom = region.pixels(width, height)
    return image[top:bottom, left:right]


def render_region_toml(region: Region) -> str:
    return (
        "[region]\n"
        f'monitor = "{region.monitor}"\n'
        f"x = {round(region.x, 4)}\n"
        f"y = {round(region.y, 4)}\n"
        f"width = {round(region.width, 4)}\n"
        f"height = {round(region.height, 4)}\n"
    )


def calibrate(capture: PortalCapture, monitor: str) -> Region:
    frame = next(iter(capture.frames()))
    height, width = frame.image.shape[:2]
    selection = cv2.selectROI(CALIBRATION_WINDOW, frame.image, showCrosshair=False)
    cv2.destroyAllWindows()
    left, top, box_width, box_height = selection
    if box_width == 0 or box_height == 0:
        raise ValueError("зону не виділено")
    return Region(
        monitor=monitor,
        x=left / width,
        y=top / height,
        width=box_width / width,
        height=box_height / height,
    )
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_region.py -v`
Expected: PASS, 3 тести

- [ ] **Step 5: Створити `cli.py` із командою калібрування**

```python
from __future__ import annotations

import argparse
import sys
from pathlib import Path

from wuwa_ua.capture.portal import PortalCapture
from wuwa_ua.config import load_config
from wuwa_ua.region import calibrate, render_region_toml

CONFIG_PATH = Path.home() / ".config" / "wuwa-ua" / "config.toml"


def cmd_calibrate(args: argparse.Namespace) -> int:
    token = ""
    if CONFIG_PATH.is_file():
        token = load_config(CONFIG_PATH).restore_token
    capture = PortalCapture(restore_token=token)
    capture.start()
    region = calibrate(capture, args.monitor)
    capture.stop()
    print(render_region_toml(region))
    print(f"# restore_token = \"{capture.restore_token}\"")
    print(f"# скопіюй у {CONFIG_PATH}")
    return 0


def main() -> int:
    parser = argparse.ArgumentParser(prog="wuwa-ua")
    subparsers = parser.add_subparsers(dest="command", required=True)
    calibrate_parser = subparsers.add_parser("calibrate", help="виділити зону субтитрів")
    calibrate_parser.add_argument("--monitor", required=True, help="connector-імʼя екрана гри, напр. DP-4")
    args = parser.parse_args()
    if args.command == "calibrate":
        return cmd_calibrate(args)
    return 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 6: Відкалібрувати на реальній грі**

Запустити гру, довести до діалогової сцени, у другому терміналі виконати (`kscreen-doctor -o` покаже connector-імена):

Run: `python -m wuwa_ua.cli calibrate --monitor DP-4`

Виділити плашку субтитрів, натиснути Enter, скопіювати надрукований блок `[region]` і `restore_token` у `~/.config/wuwa-ua/config.toml`.

- [ ] **Step 7: Стейдж**

```bash
git add src/wuwa_ua/region.py src/wuwa_ua/cli.py tests/test_region.py
```

---

## Фаза 4 — вивід і керування

### Task 10: Вікно на сусідньому моніторі

**Files:**
- Create: `src/wuwa_ua/display/__init__.py`, `src/wuwa_ua/display/base.py`, `src/wuwa_ua/display/window.py`

**Interfaces:**
- Consumes: `SubtitleLine` із `types.py`
- Produces: `Display` (Protocol із `show(line: SubtitleLine) -> None` і `clear() -> None`), `WindowDisplay(monitor: str, history_size: int = 8)` з тими самими методами плюс `run() -> None`

- [ ] **Step 1: Реалізувати `base.py` і `window.py`**

`src/wuwa_ua/display/__init__.py` лишається порожнім.

`src/wuwa_ua/display/base.py`:

```python
from __future__ import annotations

from typing import Protocol

from wuwa_ua.types import SubtitleLine


class Display(Protocol):
    def show(self, line: SubtitleLine) -> None: ...
    def clear(self) -> None: ...
    def run(self) -> None: ...
```

`src/wuwa_ua/display/window.py`:

```python
from __future__ import annotations

import gi

gi.require_version("Gtk", "4.0")
from gi.repository import GLib, Gtk  # noqa: E402

from wuwa_ua.types import SubtitleLine  # noqa: E402

STYLE = b"""
window { background-color: #101014; }
label.current { color: #f2f2f5; font-size: 26px; font-weight: 600; }
label.source { color: #8a8a96; font-size: 15px; font-style: italic; }
label.history { color: #6f6f7a; font-size: 15px; }
label.untranslated { color: #d8a657; }
"""


class WindowDisplay:
    def __init__(self, monitor: str, history_size: int = 8) -> None:
        self._monitor = monitor
        self._history_size = history_size
        self._app = Gtk.Application(application_id="io.github.wuwaua")
        self._app.connect("activate", self._on_activate)
        self._current = Gtk.Label(label="", wrap=True, xalign=0.0)
        self._source = Gtk.Label(label="", wrap=True, xalign=0.0)
        self._history_box = Gtk.Box(orientation=Gtk.Orientation.VERTICAL, spacing=6)
        self._history: list[str] = []

    def run(self) -> None:
        self._app.run(None)

    def show(self, line: SubtitleLine) -> None:
        GLib.idle_add(self._apply, line)

    def clear(self) -> None:
        GLib.idle_add(self._apply, SubtitleLine(source="", translation="", translated=True, timestamp=0.0))

    def _apply(self, line: SubtitleLine) -> bool:
        if self._current.get_label():
            self._push_history(self._current.get_label())
        self._current.set_label(line.translation)
        self._source.set_label(line.source)
        classes = ["current"] if line.translated else ["current", "untranslated"]
        self._current.set_css_classes(classes)
        return False

    def _push_history(self, text: str) -> None:
        self._history.append(text)
        while len(self._history) > self._history_size:
            self._history.pop(0)
        child = self._history_box.get_first_child()
        while child is not None:
            self._history_box.remove(child)
            child = self._history_box.get_first_child()
        for entry in reversed(self._history):
            label = Gtk.Label(label=entry, wrap=True, xalign=0.0)
            label.set_css_classes(["history"])
            self._history_box.append(label)

    def _on_activate(self, app: Gtk.Application) -> None:
        provider = Gtk.CssProvider()
        provider.load_from_data(STYLE)
        window = Gtk.ApplicationWindow(application=app, title="wuwa-ua")
        window.set_default_size(760, 520)
        Gtk.StyleContext.add_provider_for_display(
            window.get_display(), provider, Gtk.STYLE_PROVIDER_PRIORITY_APPLICATION
        )
        self._current.set_css_classes(["current"])
        self._source.set_css_classes(["source"])
        layout = Gtk.Box(orientation=Gtk.Orientation.VERTICAL, spacing=14)
        layout.set_margin_top(18)
        layout.set_margin_bottom(18)
        layout.set_margin_start(20)
        layout.set_margin_end(20)
        layout.append(self._current)
        layout.append(self._source)
        layout.append(Gtk.Separator())
        scroller = Gtk.ScrolledWindow(vexpand=True)
        scroller.set_child(self._history_box)
        layout.append(scroller)
        window.set_child(layout)
        window.present()
```

- [ ] **Step 2: Перевірити вікно вручну**

```bash
python - <<'EOF'
import threading, time
from wuwa_ua.display.window import WindowDisplay
from wuwa_ua.types import SubtitleLine

display = WindowDisplay(monitor="DP-5")

def feed() -> None:
    time.sleep(1.5)
    for index, (source, translation) in enumerate([
        ("We should go.", "Нам треба йти."),
        ("Rover, wait for me here.", "Мандрівнику, зачекай тут."),
        ("Something is wrong.", "Щось не так."),
    ]):
        display.show(SubtitleLine(source, translation, True, float(index)))
        time.sleep(1.5)
    display.show(SubtitleLine("Untranslated line.", "Untranslated line.", False, 9.0))

threading.Thread(target=feed, daemon=True).start()
display.run()
EOF
```

Очікувано: зʼявляється темне вікно; репліки змінюються кожні 1.5 с, попередні спускаються в історію; остання показана жовтим як неперекладена.

- [ ] **Step 3: Стейдж**

```bash
git add src/wuwa_ua/display/__init__.py src/wuwa_ua/display/base.py src/wuwa_ua/display/window.py
```

---

### Task 11: Оверлей поверх гри

**Files:**
- Create: `src/wuwa_ua/display/overlay.py`

**Interfaces:**
- Consumes: `SubtitleLine` із `types.py`
- Produces: `OverlayDisplay(monitor: str, bottom_margin: int = 220)` з методами `show`, `clear`, `run`

- [ ] **Step 1: Реалізувати `overlay.py`**

```python
from __future__ import annotations

import gi

gi.require_version("Gtk", "4.0")
gi.require_version("Gtk4LayerShell", "1.0")
from gi.repository import GLib, Gtk  # noqa: E402
from gi.repository import Gtk4LayerShell as LayerShell  # noqa: E402

from wuwa_ua.types import SubtitleLine  # noqa: E402

STYLE = b"""
window { background-color: transparent; }
label.overlay {
    color: #ffffff;
    font-size: 24px;
    font-weight: 600;
    background-color: rgba(0, 0, 0, 0.62);
    padding: 10px 18px;
    border-radius: 10px;
}
label.untranslated { color: #d8a657; }
"""


class OverlayDisplay:
    def __init__(self, monitor: str, bottom_margin: int = 220) -> None:
        self._monitor = monitor
        self._bottom_margin = bottom_margin
        self._app = Gtk.Application(application_id="io.github.wuwaua.overlay")
        self._app.connect("activate", self._on_activate)
        self._label = Gtk.Label(label="", wrap=True, justify=Gtk.Justification.CENTER)

    def run(self) -> None:
        self._app.run(None)

    def show(self, line: SubtitleLine) -> None:
        GLib.idle_add(self._apply, line)

    def clear(self) -> None:
        GLib.idle_add(self._apply, SubtitleLine(source="", translation="", translated=True, timestamp=0.0))

    def _apply(self, line: SubtitleLine) -> bool:
        self._label.set_label(line.translation)
        self._label.set_visible(bool(line.translation))
        classes = ["overlay"] if line.translated else ["overlay", "untranslated"]
        self._label.set_css_classes(classes)
        return False

    def _on_activate(self, app: Gtk.Application) -> None:
        window = Gtk.ApplicationWindow(application=app)
        LayerShell.init_for_window(window)
        LayerShell.set_layer(window, LayerShell.Layer.OVERLAY)
        LayerShell.set_anchor(window, LayerShell.Edge.BOTTOM, True)
        LayerShell.set_margin(window, LayerShell.Edge.BOTTOM, self._bottom_margin)
        LayerShell.set_keyboard_mode(window, LayerShell.KeyboardMode.NONE)
        for monitor in window.get_display().get_monitors():
            if monitor.get_connector() == self._monitor:
                LayerShell.set_monitor(window, monitor)
                break
        provider = Gtk.CssProvider()
        provider.load_from_data(STYLE)
        Gtk.StyleContext.add_provider_for_display(
            window.get_display(), provider, Gtk.STYLE_PROVIDER_PRIORITY_APPLICATION
        )
        self._label.set_css_classes(["overlay"])
        window.set_child(self._label)
        window.present()
        window.get_surface().set_input_region(None)
```

- [ ] **Step 2: Перевірити оверлей поверх гри**

Запустити гру у режимі **borderless window** (не exclusive fullscreen), потім:

```bash
python - <<'EOF'
import threading, time
from wuwa_ua.display.overlay import OverlayDisplay
from wuwa_ua.types import SubtitleLine

display = OverlayDisplay(monitor="DP-4")

def feed() -> None:
    time.sleep(2)
    display.show(SubtitleLine("Test.", "Перевірка оверлея.", True, 0.0))

threading.Thread(target=feed, daemon=True).start()
display.run()
EOF
```

Очікувано: напис зʼявляється поверх зображення гри, не забирає фокус, кліки проходять крізь нього в гру.

Якщо оверлей не показується поверх гри — зафіксувати це як обмеження, режим `window` лишається основним, і повідомити замовнику.

- [ ] **Step 3: Стейдж**

```bash
git add src/wuwa_ua/display/overlay.py
```

---

### Task 12: Сокет керування

**Files:**
- Create: `src/wuwa_ua/control.py`
- Test: `tests/test_control.py`

**Interfaces:**
- Consumes: нічого
- Produces: `ControlServer(path: Path, handler: Callable[[str], str])` з методами `start() -> None`, `stop() -> None`; `send_command(path: Path, command: str) -> str`; `COMMANDS: tuple[str, ...]`

- [ ] **Step 1: Написати падаючі тести**

`tests/test_control.py`:

```python
from pathlib import Path

from wuwa_ua.control import ControlServer, send_command


def test_command_reaches_the_handler(tmp_path: Path) -> None:
    received: list[str] = []

    def handler(command: str) -> str:
        received.append(command)
        return "ok"

    server = ControlServer(tmp_path / "sock", handler)
    server.start()
    try:
        assert send_command(tmp_path / "sock", "pause") == "ok"
    finally:
        server.stop()

    assert received == ["pause"]


def test_handler_reply_is_returned(tmp_path: Path) -> None:
    server = ControlServer(tmp_path / "sock", lambda command: f"echo:{command}")
    server.start()
    try:
        assert send_command(tmp_path / "sock", "mode overlay") == "echo:mode overlay"
    finally:
        server.stop()


def test_stale_socket_file_is_replaced(tmp_path: Path) -> None:
    path = tmp_path / "sock"
    path.write_text("stale", encoding="utf-8")

    server = ControlServer(path, lambda command: "ok")
    server.start()
    try:
        assert send_command(path, "resume") == "ok"
    finally:
        server.stop()


def test_stop_removes_the_socket(tmp_path: Path) -> None:
    path = tmp_path / "sock"
    server = ControlServer(path, lambda command: "ok")
    server.start()
    server.stop()

    assert not path.exists()
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_control.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.control'`

- [ ] **Step 3: Реалізувати `control.py`**

```python
from __future__ import annotations

import socket
import threading
from collections.abc import Callable
from pathlib import Path

COMMANDS = ("pause", "resume", "toggle", "mode window", "mode overlay", "status")
ENCODING = "utf-8"
BUFFER = 4096


class ControlServer:
    def __init__(self, path: Path, handler: Callable[[str], str]) -> None:
        self._path = path
        self._handler = handler
        self._socket: socket.socket | None = None
        self._thread: threading.Thread | None = None
        self._running = False

    def start(self) -> None:
        self._path.parent.mkdir(parents=True, exist_ok=True)
        if self._path.exists():
            self._path.unlink()
        self._socket = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
        self._socket.bind(str(self._path))
        self._socket.listen(4)
        self._running = True
        self._thread = threading.Thread(target=self._serve, daemon=True)
        self._thread.start()

    def stop(self) -> None:
        self._running = False
        if self._socket is not None:
            self._socket.close()
            self._socket = None
        if self._path.exists():
            self._path.unlink()

    def _serve(self) -> None:
        while self._running and self._socket is not None:
            try:
                connection, _ = self._socket.accept()
            except OSError:
                return
            with connection:
                command = connection.recv(BUFFER).decode(ENCODING).strip()
                try:
                    reply = self._handler(command)
                except Exception as exc:
                    reply = f"error: {exc}"
                connection.sendall(reply.encode(ENCODING))


def send_command(path: Path, command: str) -> str:
    with socket.socket(socket.AF_UNIX, socket.SOCK_STREAM) as client:
        client.connect(str(path))
        client.sendall(command.encode(ENCODING))
        return client.recv(BUFFER).decode(ENCODING).strip()
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_control.py -v`
Expected: PASS, 4 тести

- [ ] **Step 5: Стейдж**

```bash
git add src/wuwa_ua/control.py tests/test_control.py
```

---

## Фаза 5 — збірка й налаштування на живій грі

### Task 13: Конвеєр

**Files:**
- Create: `src/wuwa_ua/pipeline.py`
- Modify: `src/wuwa_ua/cli.py` (додати команди `run`, `pause`, `resume`, `toggle`, `mode`)
- Test: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: усе попереднє
- Produces: `Pipeline(config: Config, capture, ocr, translator, cache, glossary, display)` з методами `process(crop: np.ndarray) -> SubtitleLine | None`, `run() -> None`, `handle_command(command: str) -> str`

- [ ] **Step 1: Написати падаючі тести логіки конвеєра**

`tests/test_pipeline.py`:

```python
from pathlib import Path

import numpy as np
import pytest

from wuwa_ua.config import Config
from wuwa_ua.pipeline import Pipeline
from wuwa_ua.translate.cache import TranslationCache
from wuwa_ua.translate.errors import TranslationUnavailable
from wuwa_ua.translate.glossary import Glossary
from wuwa_ua.types import OcrResult, Region, SubtitleLine


class FakeOcr:
    def __init__(self, results: list[OcrResult]) -> None:
        self._results = results
        self.calls = 0

    def recognize(self, image: np.ndarray) -> OcrResult:
        result = self._results[min(self.calls, len(self._results) - 1)]
        self.calls += 1
        return result


class FakeTranslator:
    def __init__(self, reply: str | None = "Нам треба йти.") -> None:
        self._reply = reply
        self.calls = 0

    def translate(self, text, context, entries) -> str:
        self.calls += 1
        if self._reply is None:
            raise TranslationUnavailable("down")
        return self._reply


class FakeDisplay:
    def __init__(self) -> None:
        self.shown: list[SubtitleLine] = []

    def show(self, line: SubtitleLine) -> None:
        self.shown.append(line)

    def clear(self) -> None:
        self.shown.clear()


def make_config() -> Config:
    return Config(
        region=Region(monitor="DP-4", x=0.0, y=0.0, width=1.0, height=1.0),
        sample_fps=4.0, change_threshold=3.0, stable_frames=2, min_confidence=60.0,
        translate_timeout=4.0, context_lines=3, display_mode="window",
        display_monitor="DP-5", model="test", ollama_host="http://x", restore_token="",
    )


def make_pipeline(tmp_path: Path, ocr: FakeOcr, translator: FakeTranslator, display: FakeDisplay) -> Pipeline:
    return Pipeline(
        config=make_config(), capture=None, ocr=ocr, translator=translator,
        cache=TranslationCache(tmp_path / "cache.sqlite"),
        glossary=Glossary.load(tmp_path / "missing.tsv"), display=display,
    )


def test_translates_and_shows(tmp_path: Path) -> None:
    display = FakeDisplay()
    pipeline = make_pipeline(tmp_path, FakeOcr([OcrResult("We should go.", 90.0)]), FakeTranslator(), display)

    line = pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8))

    assert line is not None
    assert line.translation == "Нам треба йти."
    assert line.translated is True
    assert display.shown[-1].translation == "Нам треба йти."


def test_low_confidence_is_dropped(tmp_path: Path) -> None:
    display = FakeDisplay()
    translator = FakeTranslator()
    pipeline = make_pipeline(tmp_path, FakeOcr([OcrResult("We should go.", 40.0)]), translator, display)

    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is None
    assert translator.calls == 0
    assert display.shown == []


def test_meaningless_text_is_dropped(tmp_path: Path) -> None:
    display = FakeDisplay()
    translator = FakeTranslator()
    pipeline = make_pipeline(tmp_path, FakeOcr([OcrResult("|| ..", 95.0)]), translator, display)

    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is None
    assert translator.calls == 0


def test_cache_hit_skips_the_translator(tmp_path: Path) -> None:
    display = FakeDisplay()
    translator = FakeTranslator()
    pipeline = make_pipeline(tmp_path, FakeOcr([OcrResult("We should go.", 90.0)]), translator, display)

    pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8))
    pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8))

    assert translator.calls == 1
    assert display.shown[-1].translation == "Нам треба йти."


def test_translator_failure_falls_back_to_source(tmp_path: Path) -> None:
    display = FakeDisplay()
    pipeline = make_pipeline(
        tmp_path, FakeOcr([OcrResult("We should go.", 90.0)]), FakeTranslator(reply=None), display
    )

    line = pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8))

    assert line is not None
    assert line.translation == "We should go."
    assert line.translated is False


def test_failed_translation_is_not_cached(tmp_path: Path) -> None:
    display = FakeDisplay()
    cache = TranslationCache(tmp_path / "cache.sqlite")
    pipeline = Pipeline(
        config=make_config(), capture=None, ocr=FakeOcr([OcrResult("We should go.", 90.0)]),
        translator=FakeTranslator(reply=None), cache=cache,
        glossary=Glossary.load(tmp_path / "missing.tsv"), display=display,
    )
    pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8))

    assert cache.get("We should go.") is None


def test_context_grows_and_is_bounded(tmp_path: Path) -> None:
    display = FakeDisplay()
    ocr = FakeOcr([OcrResult(f"Line {index}.", 90.0) for index in range(6)])
    pipeline = make_pipeline(tmp_path, ocr, FakeTranslator(), display)

    for _ in range(6):
        pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8))

    assert len(pipeline.context) == 3


def test_identical_text_is_not_emitted_twice(tmp_path: Path) -> None:
    display = FakeDisplay()
    translator = FakeTranslator()
    ocr = FakeOcr([OcrResult("We should go.", 90.0)])
    pipeline = make_pipeline(tmp_path, ocr, translator, display)

    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is not None
    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is None
    assert len(display.shown) == 1


def test_text_differing_by_one_word_is_emitted(tmp_path: Path) -> None:
    display = FakeDisplay()
    ocr = FakeOcr([OcrResult("We should go now.", 90.0), OcrResult("We should go home.", 90.0)])
    pipeline = make_pipeline(tmp_path, ocr, FakeTranslator(), display)

    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is not None
    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is not None
    assert len(display.shown) == 2


def test_pause_stops_processing(tmp_path: Path) -> None:
    display = FakeDisplay()
    pipeline = make_pipeline(tmp_path, FakeOcr([OcrResult("We should go.", 90.0)]), FakeTranslator(), display)
    pipeline.handle_command("pause")

    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is None

    pipeline.handle_command("resume")

    assert pipeline.process(np.zeros((10, 10, 3), dtype=np.uint8)) is not None


def test_unknown_command_is_reported(tmp_path: Path) -> None:
    pipeline = make_pipeline(tmp_path, FakeOcr([OcrResult("x", 90.0)]), FakeTranslator(), FakeDisplay())

    assert "невідома" in pipeline.handle_command("fly").lower()
```

- [ ] **Step 2: Переконатись, що тести падають**

Run: `python -m pytest tests/test_pipeline.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'wuwa_ua.pipeline'`

- [ ] **Step 3: Реалізувати `pipeline.py`**

```python
from __future__ import annotations

import json
import time
from collections import deque
from pathlib import Path
from typing import Any

import numpy as np

from wuwa_ua.config import Config
from wuwa_ua.detect import ChangeDetector
from wuwa_ua.normalize import cache_key, is_meaningful
from wuwa_ua.region import crop
from wuwa_ua.translate.errors import TranslationUnavailable
from wuwa_ua.types import SubtitleLine

HISTORY_PATH = Path.home() / ".local" / "share" / "wuwa-ua" / "history.jsonl"


class Pipeline:
    def __init__(
        self,
        config: Config,
        capture: Any,
        ocr: Any,
        translator: Any,
        cache: Any,
        glossary: Any,
        display: Any,
    ) -> None:
        self._config = config
        self._capture = capture
        self._ocr = ocr
        self._translator = translator
        self._cache = cache
        self._glossary = glossary
        self._display = display
        self._detector = ChangeDetector(config.change_threshold, config.stable_frames)
        self._paused = False
        self._last_key = ""
        self.context: deque[str] = deque(maxlen=config.context_lines)

    def handle_command(self, command: str) -> str:
        if command == "pause":
            self._paused = True
            return "paused"
        if command == "resume":
            self._paused = False
            return "resumed"
        if command == "toggle":
            self._paused = not self._paused
            return "paused" if self._paused else "resumed"
        if command == "status":
            return "paused" if self._paused else "running"
        return f"невідома команда: {command}"

    def process(self, region_crop: np.ndarray) -> SubtitleLine | None:
        if self._paused:
            return None

        result = self._ocr.recognize(region_crop)
        if result.confidence < self._config.min_confidence:
            return None
        if not is_meaningful(result.text):
            return None

        source = result.text
        if cache_key(source) == self._last_key:
            return None
        self._last_key = cache_key(source)

        cached = self._cache.get(source)
        if cached is not None:
            return self._emit(SubtitleLine(source, cached, True, time.time()))

        try:
            translation = self._translator.translate(
                source, list(self.context), self._glossary.entries_for(source)
            )
        except TranslationUnavailable:
            return self._emit(SubtitleLine(source, source, False, time.time()))

        self._cache.put(source, translation)
        return self._emit(SubtitleLine(source, translation, True, time.time()))

    def run(self) -> None:
        interval = 1.0 / self._config.sample_fps
        next_sample = time.monotonic()
        for frame in self._capture.frames():
            now = time.monotonic()
            if now < next_sample:
                continue
            next_sample = now + interval
            if self._paused:
                continue
            candidate = self._detector.push(crop(frame.image, self._config.region))
            if candidate is not None:
                self.process(candidate)

    def _emit(self, line: SubtitleLine) -> SubtitleLine:
        self.context.append(line.source)
        self._display.show(line)
        self._log(line)
        return line

    def _log(self, line: SubtitleLine) -> None:
        HISTORY_PATH.parent.mkdir(parents=True, exist_ok=True)
        record = {
            "timestamp": line.timestamp,
            "source": line.source,
            "translation": line.translation,
            "translated": line.translated,
        }
        with HISTORY_PATH.open("a", encoding="utf-8") as handle:
            handle.write(json.dumps(record, ensure_ascii=False) + "\n")
```

- [ ] **Step 4: Переконатись, що тести проходять**

Run: `python -m pytest tests/test_pipeline.py -v`
Expected: PASS, 9 тестів

- [ ] **Step 5: Додати команду `run` та клієнтські команди в `cli.py`**

Дописати в `src/wuwa_ua/cli.py`:

```python
import os
import threading

from wuwa_ua.control import ControlServer, send_command
from wuwa_ua.display.overlay import OverlayDisplay
from wuwa_ua.display.window import WindowDisplay
from wuwa_ua.ocr.tesseract import TesseractOcr
from wuwa_ua.pipeline import Pipeline
from wuwa_ua.translate.cache import TranslationCache
from wuwa_ua.translate.glossary import Glossary
from wuwa_ua.translate.ollama import OllamaTranslator

GLOSSARY_PATH = Path.home() / ".config" / "wuwa-ua" / "glossary.tsv"
CACHE_PATH = Path.home() / ".local" / "share" / "wuwa-ua" / "cache.sqlite"


def socket_path() -> Path:
    runtime = os.environ.get("XDG_RUNTIME_DIR", "/tmp")
    return Path(runtime) / "wuwa-ua.sock"


def cmd_run(args: argparse.Namespace) -> int:
    config = load_config(CONFIG_PATH)
    capture = PortalCapture(restore_token=config.restore_token)
    capture.start()

    if config.display_mode == "overlay":
        display = OverlayDisplay(monitor=config.region.monitor)
    else:
        display = WindowDisplay(monitor=config.display_monitor)

    pipeline = Pipeline(
        config=config,
        capture=capture,
        ocr=TesseractOcr(min_confidence=config.min_confidence),
        translator=OllamaTranslator(config.model, config.ollama_host, config.translate_timeout),
        cache=TranslationCache(CACHE_PATH),
        glossary=Glossary.load(GLOSSARY_PATH),
        display=display,
    )

    server = ControlServer(socket_path(), pipeline.handle_command)
    server.start()
    threading.Thread(target=pipeline.run, daemon=True).start()
    try:
        display.run()
    finally:
        server.stop()
        capture.stop()
    return 0


def cmd_send(args: argparse.Namespace) -> int:
    print(send_command(socket_path(), args.command_text))
    return 0
```

І замінити тіло `main()` на:

```python
def main() -> int:
    parser = argparse.ArgumentParser(prog="wuwa-ua")
    subparsers = parser.add_subparsers(dest="command", required=True)
    subparsers.add_parser("calibrate", help="виділити зону субтитрів")
    subparsers.add_parser("run", help="запустити демон")
    for name in ("pause", "resume", "toggle", "status"):
        subparsers.add_parser(name, help=f"надіслати команду {name}")
    args = parser.parse_args()

    if args.command == "calibrate":
        return cmd_calibrate(args)
    if args.command == "run":
        return cmd_run(args)
    args.command_text = args.command
    return cmd_send(args)
```

- [ ] **Step 6: Переконатись, що весь набір тестів зелений**

Run: `python -m pytest -v`
Expected: PASS, усі тести

- [ ] **Step 7: Стейдж**

```bash
git add src/wuwa_ua/pipeline.py src/wuwa_ua/cli.py tests/test_pipeline.py
```

---

### Task 14: Вибір моделі під реальну VRAM і фінальне налаштування

**Files:**
- Create: `scripts/bench_models.py`, `README.md`
- Modify: `~/.config/wuwa-ua/config.toml` (поле `model`)

**Interfaces:**
- Consumes: `OllamaTranslator` із `translate/ollama.py`
- Produces: значення `model` у конфізі; `README.md` із інструкцією запуску

- [ ] **Step 1: Заміряти вільну VRAM під час гри**

Запустити гру, зайти в діалогову сцену, у другому терміналі:

```bash
nvidia-smi --query-gpu=memory.total,memory.used,memory.free --format=csv -l 2 -c 5
```

Записати мінімальне значення `memory.free`. Це бюджет під модель.

- [ ] **Step 2: Завантажити моделі-кандидати**

Обрати кандидатів за бюджетом з кроку 1: якщо вільно ≥ 4.5 ГБ — `gemma3:4b` і `qwen3:4b`; якщо 2.5–4.5 ГБ — `gemma3:1b` і `qwen3:1.7b`; якщо < 2.5 ГБ — тільки CPU-режим із `gemma3:4b`.

```bash
systemctl --user start ollama
ollama pull gemma3:4b
ollama pull qwen3:4b
```

- [ ] **Step 3: Написати бенчмарк**

`scripts/bench_models.py`:

```python
from __future__ import annotations

import statistics
import sys
import time

from wuwa_ua.translate.errors import TranslationUnavailable
from wuwa_ua.translate.ollama import OllamaTranslator

SAMPLES = [
    "We should go. The Tacet Discord is getting closer.",
    "Rover, wait for me here. I'll check the perimeter.",
    "You don't remember any of it, do you?",
    "The Sonoro Sphere resonates with the frequency of this place.",
    "I never asked for your protection.",
]
ENTRIES = [("Rover", "Мандрівник"), ("Tacet Discord", "Тацетний Дискорд")]


def main() -> int:
    models = sys.argv[1:]
    if not models:
        print("вкажи моделі: python scripts/bench_models.py gemma3:4b qwen3:4b")
        return 1

    for model in models:
        translator = OllamaTranslator(model=model, timeout=30.0)
        durations: list[float] = []
        print(f"\n=== {model}")
        for sample in SAMPLES:
            started = time.monotonic()
            try:
                translation = translator.translate(sample, context=[], entries=ENTRIES)
            except TranslationUnavailable as exc:
                print(f"  ПОМИЛКА: {exc}")
                continue
            elapsed = time.monotonic() - started
            durations.append(elapsed)
            print(f"  {elapsed:5.2f}s  {translation}")
        if durations:
            print(f"  медіана: {statistics.median(durations):.2f}s  максимум: {max(durations):.2f}s")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: Прогнати бенчмарк під час гри**

Гра має бути запущена й займати VRAM — інакше замір нічого не вартий.

Run: `python scripts/bench_models.py gemma3:4b qwen3:4b`

Критерій вибору: медіана ≤ 2.0 с при прийнятній якості українського тексту (читає людина, не тест). Якщо жодна модель не вкладається — перейти на CPU: `OLLAMA_NUM_GPU=0 systemctl --user restart ollama` і повторити замір.

Записати переможця у `~/.config/wuwa-ua/config.toml`:

```toml
[translate]
model = "переможець"
timeout = 4.0
context_lines = 3
```

- [ ] **Step 5: Прогін на живій грі**

```bash
python -m wuwa_ua.cli run
```

Перевірити в діалоговій сцені:
- переклад зʼявляється протягом ~2 с після появи репліки;
- обірваних півфраз немає (працює гейт стабілізації);
- повторні репліки зʼявляються миттєво (працює кеш);
- імена з глосарію перекладені однаково;
- `wuwa-ua pause` / `wuwa-ua resume` спрацьовують.

За результатом підкрутити в конфізі `change_threshold`, `stable_frames`, `min_confidence` і зону `[region]`.

- [ ] **Step 6: Повісити гарячі клавіші KDE**

`Системні налаштування → Клавіатура → Скорочення → Додати команду`:
- `wuwa-ua toggle` → напр. `Meta+Shift+T`
- `wuwa-ua status` → за бажанням

Перевірити, що спрацьовує поверх запущеної гри.

- [ ] **Step 7: Написати `README.md`**

Має містити: призначення, що встановити (`pacman` рядок із Task 1 + `pip install -e .`), як відкалібрувати зону, як запустити, як правити глосарій, як перемикати режим виводу, і явну примітку, що інструмент не торкається файлів і процесу гри.

- [ ] **Step 8: Стейдж**

```bash
git add scripts/bench_models.py README.md
```

---

## Самоперевірка плану

**Покриття специфікації.** Захоплення через портал — Task 8; зони й калібрування — Task 9; детектор змін і гейт стабілізації — Task 2; OCR із препроцесингом і замінним бекендом — Task 7; нормалізація — Task 3; кеш — Task 4; глосарій і промпт — Task 5; ollama з таймаутом — Task 6; вивід у двох режимах — Tasks 10-11; керування через сокет і хоткеї KDE — Tasks 12 і 14; таблиця збоїв — Tasks 6 і 13; вибір моделі за реальною VRAM — Task 14; харнес точності OCR — Task 7.

**Свідомі відхилення від шаблону.** Кроки завершуються `git add`, а не комітом. Бекенд `ocr/vlm.py` зі специфікації тут задачею не оформлений: він потрібен лише якщо харнес у Task 7 покаже частку помилок > 10%, і планувати його наперед було б передчасно — точка рішення й дія за обома результатами описані в Task 7 Step 6.

**Ризики, які план не усуває.** Точність tesseract на шрифті гри невідома до Task 7 Step 6. Робота layer-shell оверлея поверх гри під Proton невідома до Task 11 Step 2; запасний варіант — режим `window`, який від цього не залежить.
